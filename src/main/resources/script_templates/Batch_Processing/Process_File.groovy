/**
 * This script illustrates how to process multiple files
 * using the SciJava batch processor.
 *
 * It takes a File input and processes a single file.
 * To apply it to multiple files, use one of the following:
 *
 * * Use the Batch button in the script editor
 * * Save the script in a subfolder of /scripts/, restart ImageJ,
 *   find the script using the search bar and its the Batch button
 * * Process > Batch > Run Script from File
 * * Process > Batch > Run Script from Menu
 */

#@ File (style="extensions:tif/tiff") tifFile
